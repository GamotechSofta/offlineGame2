{
  "name": "games",
  "version": "1.0.0",
  "private": true,
  "main": "ExpoEntry.js",
  "scripts": {
    "install": "cd frontend && npm install",
    "build": "cd frontend && npm run build && cp -r dist ../dist",
    "start": "cd frontend && npm run preview",
    "mobile": "cd mobile && npx expo start --clear --port 8082"
  },
  "engines": {
    "node": "22.22.0"
  },
  "dependencies": {
    "expo": "^54.0.33",
    "firebase": "^12.8.0"
  }
}
