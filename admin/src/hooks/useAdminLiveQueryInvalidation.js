/**
 * Previously: admin socket → React Query invalidations.
 * Intentionally disabled to reduce backend load.
 */
const useAdminLiveQueryInvalidation = () => {};

export default useAdminLiveQueryInvalidation;
