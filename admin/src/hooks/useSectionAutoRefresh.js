/**
 * Previously: global-ticker + visibility-driven periodic refresh (heavy on backend).
 * Intentionally disabled — data loads on mount and explicit user actions only.
 */
const useSectionAutoRefresh = () => {};

export default useSectionAutoRefresh;
